FROM python:3.12-slim
WORKDIR /app
COPY app.py rnaseq_index.sqlite sample_metadata.csv ./
COPY web ./web
EXPOSE 8765
ENV DB_PATH=/app/rnaseq_index.sqlite
CMD ["sh", "-c", "python app.py --host 0.0.0.0 --port ${PORT:-8765}"]
